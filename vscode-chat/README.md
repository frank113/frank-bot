# VSCode Chat

This contains the configuration for the chat features built into vscode.

As of now I am using the extension Continue for my AI needs rather than the VSCode internals. I am keeping this folder around if I ever revert that deicision.

The rationale for preferring Continue at this time is:

1. Better auto-completion capabilities
2. Enhanced model customizations via `config.yaml`
